name: build-images

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:
  schedule:
    # Weekly rebuild to pick up upstream package updates even when nosi hasn't
    # changed. Sunday 03:00 UTC, well clear of weekday CI traffic.
    - cron: '0 3 * * 0'

permissions:
  contents: read
  packages: write    # GHCR push on publish triggers

concurrency:
  group: build-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

env:
  PUBLISH: ${{ github.event_name != 'pull_request' }}

jobs:
  build:
    runs-on: ubuntu-24.04
    # The matrix is per-BASE (headless). Each leg bakes its headless base
    # once, then derive_publish builds that base's derived shapes from the
    # same qcow2 in-job (no re-bake, no cross-job artifact passing): the
    # ubuntu-2604 leg also emits wsl + docker, the fedora-44 leg also emits
    # desktop. The derive's qemu-nbd + chroot + strip + (docker import /
    # tar / gzip) adds ~10-20 min on the bases that have derivations, hence
    # the uniform 90 min ceiling.
    timeout-minutes: 90
    strategy:
      fail-fast: false
      matrix:
        variant: [debian-13-headless, ubuntu-2404-headless, ubuntu-2604-headless, fedora-44-headless, freebsd-14-headless, freebsd-15-headless]

    steps:
      - uses: actions/checkout@v5

      - name: Reclaim runner disk space on /
        # Artifacts under ~/system_imaging land on the roomy /mnt disk, but
        # everything else the build writes stays on the cramped / (~14 GiB
        # free on hosted ubuntu-24.04): cijoe/cijoe-output/ captures the
        # bake's full serial console (package_upgrade + ~150-package install)
        # plus the smoketest VM console, and the docker shape's `docker
        # import` lands image layers in /var/lib/docker. Together they
        # overrun /, surfacing as a cijoe log-flush ENOSPC mid-build. Drop
        # the preinstalled toolchains we never use (.NET, GHC/Haskell,
        # Android SDK, CodeQL); the Android SDK alone is the bulk, and the
        # df brackets below report the actual reclaim per runner image
        # (it drifts with every runner-images release).
        run: |
          df -h / | tail -1
          sudo rm -rf \
            /usr/share/dotnet \
            /opt/ghc \
            /usr/local/.ghcup \
            /usr/local/lib/android \
            /opt/hostedtoolcache/CodeQL
          sudo docker image prune --all --force >/dev/null 2>&1 || true
          df -h / | tail -1

      - name: Install build dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends \
            qemu-system-x86 \
            qemu-utils \
            genisoimage \
            pigz \
            pipx \
            cpu-checker \
            xz-utils \
            sshpass \
            python3-yaml

      - name: Stage build artifacts on the large /mnt disk
        # Hosted ubuntu-24.04 runners leave only ~14 GiB free on / but mount
        # a ~70 GiB ephemeral disk at /mnt. The build writes the qcow2, the
        # raw intermediate, the .img.gz and (wsl shape) the WSL tarball under
        # ~/system_imaging; img_gz_publish's qcow2 -> raw -> gzip needs all
        # three on disk at once and overruns /. Point ~/system_imaging at
        # /mnt so the multi-GiB artifacts land on the roomy disk. Done
        # before the cache restore so the cached cloud image lands there too.
        run: |
          sudo mkdir -p /mnt/system_imaging
          sudo chown "$(id -un):$(id -gn)" /mnt/system_imaging
          ln -s /mnt/system_imaging "$HOME/system_imaging"

      - name: Cache upstream cloud image
        # Cloud images are 500MB-2GB downloads; on a fresh runner that's the
        # dominant cost of a cold build. Key on the variant's TOML so a
        # cloud.url change automatically busts the cache. No restore-keys
        # fallback: a stale cache from a prior URL would satisfy
        # diskimage_build's path-existence check and the bake would proceed
        # with the wrong upstream.
        uses: actions/cache@v4
        with:
          path: ~/system_imaging/cloud
          key: cloud-${{ matrix.variant }}-${{ hashFiles(format('cijoe/configs/{0}.toml', matrix.variant)) }}

      - name: Enable KVM access for the runner
        # GHA hosted runners ship /dev/kvm but the runner user isn't in the
        # kvm group, so QEMU's `-accel kvm` fails. Same udev workaround bty
        # uses in its release workflow.
        run: |
          echo 'KERNEL=="kvm", GROUP="kvm", MODE="0666", OPTIONS+="static_node=kvm"' \
              | sudo tee /etc/udev/rules.d/99-kvm4all.rules
          sudo udevadm control --reload-rules
          sudo udevadm trigger --name-match=kvm
          sudo kvm-ok || true
          ls -l /dev/kvm

      - name: Install cijoe
        run: |
          pipx install cijoe
          echo "$HOME/.local/bin" >> "$GITHUB_PATH"

      # The build is four discrete cijoe phases; running them as separate
      # GHA steps gives each its own duration in the run summary AND points
      # a failure straight at the failing phase rather than at the
      # 30+ minute aggregate. `cijoe ... <step-name>` invokes just the named
      # task from cijoe/tasks/build.yaml; all four still emit into the same
      # cijoe-output/ tree, which the upload step below picks up.

      - name: Bake ${{ matrix.variant }} (cloud-init -> apply.sh -> poweroff)
        run: |
          cd cijoe && cijoe tasks/build.yaml --monitor \
            -c configs/${{ matrix.variant }}.toml diskimage_build

      - name: Publish .img.gz (qcow2 -> raw -> gzip)
        run: |
          cd cijoe && cijoe tasks/build.yaml --monitor \
            -c configs/${{ matrix.variant }}.toml img_gz_publish

      - name: Derive shapes (desktop / wsl / docker on supported bases)
        run: |
          cd cijoe && cijoe tasks/build.yaml --monitor \
            -c configs/${{ matrix.variant }}.toml derive_publish

      - name: Smoketest (boot overlay + assert)
        run: |
          cd cijoe && cijoe tasks/build.yaml --monitor \
            -c configs/${{ matrix.variant }}.toml image_smoketest

      - name: Show built-image sizes
        # Runs on every event (including PRs) and even if a later step
        # fails -- so the base qcow2 + .img.gz and every derived artifact
        # (.tar.gz for wsl, derived .img.gz for desktop) are visible before
        # the upload + GHCR-push. The docker shape lands in the local docker
        # daemon rather than a file, so it's reported via `docker images`.
        if: always()
        env:
          IMG: ${{ matrix.variant }}
        run: |
          set -eu
          cd "$HOME/system_imaging/disk"
          {
            echo "### \`${IMG}\` built-image sizes"
            echo
            echo "| File | Size | Bytes |"
            echo "|---|---:|---:|"
            for f in nosi-*.qcow2 nosi-*.img.gz nosi-*.tar.gz; do
                [ -e "$f" ] || continue
                bytes=$(stat -c%s "$f")
                printf '| `%s` | %s | %s |\n' \
                    "$f" "$(numfmt --to=iec "$bytes")" "$bytes"
            done
            echo
            echo "Local OCI images (docker shape):"
            echo '```'
            docker images 'nosi-*' 2>/dev/null || true
            echo '```'
          } | tee -a "${GITHUB_STEP_SUMMARY:-/dev/stderr}"

      - name: Upload baked image + cijoe report (always)
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: nosi-${{ matrix.variant }}
          path: |
            ~/system_imaging/disk/nosi-${{ matrix.variant }}-x86_64.qcow2
            ~/system_imaging/disk/nosi-${{ matrix.variant }}-x86_64.qcow2.sha256
            ~/system_imaging/disk/nosi-*.img.gz
            ~/system_imaging/disk/nosi-*.img.gz.sha256
            ~/system_imaging/disk/nosi-*.tar.gz
            ~/system_imaging/disk/nosi-*.tar.gz.sha256
            ~/system_imaging/disk/nosi-*.metadata.json
            ~/system_imaging/disk/nosi-${{ matrix.variant }}-x86_64.cloud-init-output.log
            ~/system_imaging/disk/nosi-${{ matrix.variant }}-x86_64.cloud-init.log
            ~/system_imaging/disk/nosi-${{ matrix.variant }}-x86_64.nosi-release
            cijoe/cijoe-output/
          if-no-files-found: warn
          retention-days: 14

      # ----- publish (push-to-main, schedule, manual) -----

      - name: Compute rolling tag
        if: env.PUBLISH == 'true'
        id: tag
        env:
          SHA: ${{ github.sha }}
        run: |
          # Use GHA-provided SHA rather than `git rev-parse`: cijoe ran parts
          # of the build as root, which trips git's safe.directory check
          # ("dubious ownership in /__w/nosi/nosi") when the rolling-tag step
          # then runs as the runner user.
          date_tag=$(date -u +'%Y.%m.%d')
          short=${SHA:0:7}
          echo "rolling=${date_tag}-${short}" >> "$GITHUB_OUTPUT"

      - name: Set up oras
        if: env.PUBLISH == 'true'
        uses: oras-project/setup-oras@v1

      - name: Log in to GHCR
        # Both clients: oras for the disk-image / wsl-rootfs artifacts,
        # docker for the docker-shape OCI image (`docker push`).
        if: env.PUBLISH == 'true'
        env:
          GHCR_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GHCR_USER: ${{ github.actor }}
        run: |
          printf '%s' "$GHCR_TOKEN" \
            | oras login ghcr.io -u "$GHCR_USER" --password-stdin
          printf '%s' "$GHCR_TOKEN" \
            | docker login ghcr.io -u "$GHCR_USER" --password-stdin

      - name: Bundle cijoe report into a zip
        # Ship the build provenance (report.html + per-step cmd_*.output
        # capturing the bake's serial console, the smoketest VM's serial
        # console, etc.) as an extra ORAS layer so consumers can
        # `oras pull` the image and inspect exactly how it was baked.
        # Nobody else does this except for Docker images via labels, but
        # raw provenance is far more useful than a label string.
        if: env.PUBLISH == 'true'
        env:
          IMG: ${{ matrix.variant }}
        run: |
          set -eu
          report="$HOME/system_imaging/disk/nosi-${IMG}-x86_64.cijoe-report.zip"
          (cd cijoe/cijoe-output && zip -qr "${report}" .)
          sha256sum "${report}" > "${report}.sha256"
          ls -lh "${report}"

      - name: Publish base + derived shapes to GHCR
        # One step pushes the headless base AND every shape derive_publish
        # produced for it. Disk-image (.img.gz) + wsl-rootfs (.tar.gz) go
        # via oras (each its own repo ghcr.io/<repo>/<variant>, so bty's
        # flashable catalog stays scoped to real disk images). The docker
        # shape is a real OCI image (docker import in derive_publish), so
        # it's `docker push`ed for `docker pull` / GHA `container:` use.
        # Annotations come from each artifact's OWN metadata.json (the base
        # smoketest extracted the base's; derive_publish exported each
        # derived one), description from variants.yml via gen_catalog.
        if: env.PUBLISH == 'true'
        env:
          REPO: ${{ github.repository }}
          IMG:  ${{ matrix.variant }}
          ROLLING: ${{ steps.tag.outputs.rolling }}
          SHA: ${{ github.sha }}
        run: |
          set -eu
          # GHA's working-directory: doesn't expand ~, hence the cd; push
          # by basename so ORAS records plain filenames (not host paths).
          cd "$HOME/system_imaging/disk"
          report="nosi-${IMG}-x86_64.cijoe-report.zip"
          gc="python3 $GITHUB_WORKSPACE/tools/gen_catalog.py"

          # oras push with the standard nosi annotation set + idempotent
          # retry (ghcr.io intermittently resets the HTTP/2 upload stream;
          # oras skips already-uploaded blobs by digest, so re-running
          # resumes). $1 ref $2 artifact-type $3 layer-media $4 file
          # $5 metadata.json $6 description.
          oras_push() {
              local ref=$1 atype=$2 lmedia=$3 file=$4 meta=$5 desc=$6
              jq -e . "$meta" >/dev/null
              local variant shape distro distro_version kernel_release built arch
              variant=$(jq -r '.nosi.variant // empty' "$meta")
              shape=$(jq -r '.nosi.shape // empty' "$meta")
              distro=$(jq -r '.distro.id // empty' "$meta")
              distro_version=$(jq -r '.distro.version_id // empty' "$meta")
              kernel_release=$(jq -r '.kernel.release // empty' "$meta")
              built=$(jq -r '.nosi.built // empty' "$meta")
              arch=$(jq -r '.architecture // empty' "$meta")
              local attempts=0
              until oras push "$ref" \
                  --artifact-type "$atype" \
                  --annotation "org.opencontainers.image.title=nosi-${variant}" \
                  --annotation "org.opencontainers.image.description=${desc}" \
                  --annotation "org.opencontainers.image.version=${ROLLING}" \
                  --annotation "org.opencontainers.image.created=${built}" \
                  --annotation "org.opencontainers.image.revision=${SHA}" \
                  --annotation "org.opencontainers.image.source=https://github.com/${REPO}" \
                  --annotation "org.opencontainers.image.url=https://github.com/${REPO}" \
                  --annotation "org.opencontainers.image.architecture=${arch}" \
                  --annotation "org.opencontainers.image.os=linux" \
                  --annotation "dev.nosi.variant=${variant}" \
                  --annotation "dev.nosi.shape=${shape}" \
                  --annotation "dev.nosi.distro=${distro}" \
                  --annotation "dev.nosi.distro-version=${distro_version}" \
                  --annotation "dev.nosi.kernel-release=${kernel_release}" \
                  "${file}:${lmedia}" \
                  "${file}.sha256:text/plain" \
                  "${report}:application/vnd.nosi.build-report.v1+zip" \
                  "${report}.sha256:text/plain" \
                  "${meta}:application/vnd.nosi.metadata.v1+json"; do
                  attempts=$((attempts + 1))
                  if [ $attempts -ge 3 ]; then
                      echo "::error::oras push $ref failed after $attempts attempts"
                      return 1
                  fi
                  echo "::warning::oras push $ref attempt $attempts failed; retrying"
                  sleep $((attempts * 15))
              done
              oras tag "$ref" latest
          }

          # Base headless: flashable .img.gz disk-image.
          oras_push "ghcr.io/${REPO}/${IMG}:${ROLLING}" \
              application/vnd.nosi.disk-image.v1+gzip \
              application/vnd.nosi.disk-image.layer.v1+gzip \
              "nosi-${IMG}-x86_64.img.gz" \
              "nosi-${IMG}-x86_64.metadata.json" \
              "$($gc --describe "${IMG}")"

          # Derived shapes, read from the base's [[...derive]] list. Bases
          # without derivations (debian / ubuntu-2404 / freebsd) emit no
          # rows and the loop is a no-op.
          derives=$(python3 -c 'import sys,tomllib; b,ws=sys.argv[1],sys.argv[2]; d=tomllib.load(open(f"{ws}/cijoe/configs/{b}.toml","rb")); [print(e["variant"],e["output"]) for e in d["system-imaging"]["images"][f"nosi-{b}-x86_64"].get("derive",[])]' "${IMG}" "$GITHUB_WORKSPACE")
          while read -r v output; do
              [ -z "$v" ] && continue
              case "$output" in
              tar)
                  oras_push "ghcr.io/${REPO}/${v}:${ROLLING}" \
                      application/vnd.nosi.wsl-rootfs.v1+gzip \
                      application/vnd.nosi.wsl-rootfs.layer.v1+gzip \
                      "nosi-${v}.tar.gz" \
                      "nosi-${v}.metadata.json" \
                      "$($gc --describe-wsl "${v}")"
                  ;;
              img)
                  oras_push "ghcr.io/${REPO}/${v}:${ROLLING}" \
                      application/vnd.nosi.disk-image.v1+gzip \
                      application/vnd.nosi.disk-image.layer.v1+gzip \
                      "nosi-${v}-x86_64.img.gz" \
                      "nosi-${v}.metadata.json" \
                      "$($gc --describe "${v}")"
                  ;;
              oci)
                  # Real OCI image (docker import in derive_publish): docker
                  # push so consumers `docker pull` / use as a GHA container:.
                  # Title set as a LABEL at import; the catalog carries the
                  # use-case prose.
                  docker tag "nosi-${v}:latest" "ghcr.io/${REPO}/${v}:${ROLLING}"
                  docker tag "nosi-${v}:latest" "ghcr.io/${REPO}/${v}:latest"
                  docker push "ghcr.io/${REPO}/${v}:${ROLLING}"
                  docker push "ghcr.io/${REPO}/${v}:latest"
                  ;;
              *)
                  echo "::error::unknown derive output '$output' for $v"
                  exit 1
                  ;;
              esac
          done <<< "$derives"

      - name: Write build summary
        if: env.PUBLISH == 'true'
        env:
          REPO: ${{ github.repository }}
          IMG:  ${{ matrix.variant }}
          ROLLING: ${{ steps.tag.outputs.rolling }}
        run: |
          gz="$HOME/system_imaging/disk/nosi-${IMG}-x86_64.img.gz"
          sha=$(awk '{print $1}' "${gz}.sha256")
          size=$(numfmt --to=iec "$(stat -c%s "${gz}")")
          report="$HOME/system_imaging/disk/nosi-${IMG}-x86_64.cijoe-report.zip"
          rsha=$(awk '{print $1}' "${report}.sha256")
          rsize=$(numfmt --to=iec "$(stat -c%s "${report}")")
          {
            echo "### \`${IMG}\` (${ROLLING})"
            echo
            echo "| | |"
            echo "|---|---|"
            echo "| Pull ref | \`ghcr.io/${REPO}/${IMG}:${ROLLING}\` |"
            echo "| Latest alias | \`ghcr.io/${REPO}/${IMG}:latest\` |"
            echo "| Blob digest (content-addressed pull) | \`sha256:${sha}\` |"
            echo "| Size | ${size} |"
            echo "| Build report (cijoe-output zip) | ${rsize}, \`sha256:${rsha}\` |"
            echo
            echo "Flash directly:"
            echo
            echo '```'
            echo "curl -sL 'https://ghcr.io/v2/${REPO}/${IMG}/blobs/sha256:${sha}' \\"
            echo "    | gunzip -d \\"
            echo "    | sudo dd of=/dev/sdX bs=4M conv=fsync status=progress"
            echo '```'
            echo
            echo "Pull the full artifact (image + build report) for provenance:"
            echo
            echo '```'
            echo "oras pull ghcr.io/${REPO}/${IMG}:${ROLLING}"
            echo "unzip nosi-${IMG}-x86_64.cijoe-report.zip -d cijoe-report"
            echo "xdg-open cijoe-report/report.html"
            echo '```'
          } >> "$GITHUB_STEP_SUMMARY"

          # Append a block per derived shape this leg published.
          derives=$(python3 -c 'import sys,tomllib; b,ws=sys.argv[1],sys.argv[2]; d=tomllib.load(open(f"{ws}/cijoe/configs/{b}.toml","rb")); [print(e["variant"],e["output"]) for e in d["system-imaging"]["images"][f"nosi-{b}-x86_64"].get("derive",[])]' "${IMG}" "$GITHUB_WORKSPACE")
          while read -r v output; do
              [ -z "$v" ] && continue
              {
                echo
                echo "### \`${v}\` (${ROLLING}, derived from ${IMG})"
                echo
                echo "| Pull ref | \`ghcr.io/${REPO}/${v}:${ROLLING}\` (alias \`:latest\`) |"
                echo "|---|---|"
                case "$output" in
                tar)
                    echo
                    echo "WSL2 rootfs -- pull the layer blob and \`wsl --import\` it." ;;
                img)
                    echo
                    echo "Flashable .img.gz -- same \`gunzip | dd\` flow as the base." ;;
                oci)
                    echo
                    echo "OCI image -- use as a GHA job container or a dev base:"
                    echo '```yaml'
                    echo "container:"
                    echo "  image: ghcr.io/${REPO}/${v}:latest"
                    echo "  options: --privileged"
                    echo '```' ;;
                esac
              } >> "$GITHUB_STEP_SUMMARY"
          done <<< "$derives"

  publish-catalog:
    # Cut a GitHub release carrying the bty-compatible catalog.toml so an
    # operator can point bty at it:
    #   bty --catalog https://github.com/safl/nosi/releases/latest/download/catalog.toml
    # Runs once (not per-variant) AFTER every matrix variant built + pushed,
    # so the catalog only advertises images that actually shipped. needs:
    # build means one failed variant leg blocks the catalog -- deliberate:
    # the catalog should not point at an image whose build broke. PRs don't
    # publish (same gate as the per-variant GHCR push).
    needs: build
    if: github.event_name != 'pull_request'
    runs-on: ubuntu-24.04
    permissions:
      contents: write    # create the release + upload the asset
    steps:
      - uses: actions/checkout@v5

      - name: Install catalog-generator deps
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends python3-yaml

      - name: Generate catalog.toml from variants.yml
        run: python3 tools/gen_catalog.py catalog.toml

      - name: Compute rolling tag
        id: tag
        env:
          SHA: ${{ github.sha }}
        run: |
          # Same scheme as the build job's rolling tag (date + short sha),
          # recomputed here so this job doesn't depend on a matrix output.
          date_tag=$(date -u +'%Y.%m.%d')
          short=${SHA:0:7}
          echo "rolling=${date_tag}-${short}" >> "$GITHUB_OUTPUT"

      - name: Publish catalog.toml as a rolling release asset
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          REPO: ${{ github.repository }}
          ROLLING: ${{ steps.tag.outputs.rolling }}
        run: |
          set -eu
          notes="Rolling nosi image catalog (schema v1). Flash nosi images with bty: bty --catalog https://github.com/${REPO}/releases/latest/download/catalog.toml -- refs are oras :latest tags, the layer digest is verified at flash time."
          # Idempotent: a re-dispatch on the same commit reuses the tag, so
          # create-or-clobber. --latest makes releases/latest/download/
          # resolve to this asset.
          if gh release view "${ROLLING}" >/dev/null 2>&1; then
              gh release upload "${ROLLING}" catalog.toml --clobber
          else
              gh release create "${ROLLING}" catalog.toml \
                  --latest \
                  --title "nosi ${ROLLING}" \
                  --notes "${notes}"
          fi
