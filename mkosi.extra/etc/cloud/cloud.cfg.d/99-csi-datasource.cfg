# Pin cloud-init to read only the NoCloud seed baked into the image at
# /var/lib/cloud/seed/nocloud/. Falling back to `None` keeps cloud-init from
# blocking on absent datasources if the seed is ever stripped.
datasource_list: [ NoCloud, None ]
